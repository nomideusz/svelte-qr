# Used only in github.com/nomideusz/svelte-qr (not the asini monorepo).
# pnpm 10+ blocks lifecycle scripts until approved; esbuild needs its postinstall.
packages:
  - '.'
allowBuilds:
  esbuild: true
